@page "{academicYear?}/{collegeGroup?}/{fac?}/{team?}/{course?}"
@model GradePredictor.Pages.FolderPaths.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<div class="container">
    <div class="row">
        <div class="col-md" id="FolderPathData">
            @Html.Raw(Model.Path)
        </div>
    </div>
</div>